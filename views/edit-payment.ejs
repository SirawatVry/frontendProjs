<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Payment</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet"/>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { font-family: 'Poppins', sans-serif; }
    </style>
</head>
<body class="bg-[#f8f3e7] text-gray-800">

    <header class="bg-[#f8f3e7] py-4">
        <div class="container mx-auto flex justify-between items-center px-4">
            <div class="text-2xl font-bold">Edit Payment</div>
            <div>
                <a href="/admin" class="bg-[#ffb84d] text-white px-4 py-2 rounded">Back to Admin</a>
            </div>
        </div>
    </header>

    <main class="container mx-auto px-4 py-8">
        <form action="/update-payment" method="POST">
            <input type="hidden" name="id" value="<%= payment.Payment_ID %>">
            <div class="mb-4">
                <label for="type" class="block text-gray-700">Payment Type</label>
                <input type="text" id="type" name="type" value="<%= payment.Payment_Type %>" required class="border rounded-lg w-full p-2" />
            </div>
            <div class="mb-4">
                <label for="amount" class="block text-gray-700">Amount</label>
                <input type="number" id="amount" name="amount" value="<%= payment.Payment_Amount %>" required class="border rounded-lg w-full p-2" />
            </div>
            <button type="submit" class="bg-[#ffb84d] text-white px-4 py-2 rounded">Update Payment</button>
        </form>
    </main>
</body>
</html>